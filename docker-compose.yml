services:
    app:
        build: ./
        command: streamlit run app/main.py --server.port 8501
        volumes:
            - ./:/app/change-requests/app
        ports:
            - 8501:8501
        image: streamlit:latest
        env_file:
            - /app/.env